#include <stdio.h>

int main() {
    char alpha;
    char a = toupper(alpha);
    if (a == alpha){
        printf("Uppercase");
    }else{
        printf("Lowercase");
    }
    return 0;
}